#include "main.h"
/**
 * _isupper - function that cheks for uppercase character
 * @c: character
 *
 * Return: 1 if c is uppercase 0 otherwise
 */
int _isupper(int c)
{
	return (c <= 90 && c >= 65);
}
